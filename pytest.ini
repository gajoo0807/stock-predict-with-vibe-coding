[pytest]
addopts = -q
# Limit test discovery to services/* to avoid picking up unrelated files
testpaths = services
# Ensure top-level package imports like `services.market_data.*` resolve
pythonpath = .
